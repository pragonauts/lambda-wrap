'use strict';

const { assert } = require('chai');

describe('LambdaWrap', () => {
    describe('new', () => {
        it('does something', () => {
            assert.equal(2, 1 + 1);
        });
    });
});
